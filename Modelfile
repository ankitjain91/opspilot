# QWEN 2.5 14B - OPTIMIZED FOR KUBERNETES DEBUGGING
# 14B parameter model - more capable than 8B, can handle more complex reasoning
FROM qwen2.5:14b

# OPTIMIZED PARAMETERS FOR 14B MODEL
# Temperature 0.2: Lower than 8B since 14B is more capable and less prone to loops
PARAMETER temperature 0.2

# Top-p (nucleus sampling): Consider top 90% probability mass
PARAMETER top_p 0.9

# Top-k: Limit to top 50 tokens (slightly higher than 8B due to better reasoning)
PARAMETER top_k 50

# Repeat penalty: Discourage repeating the same tool/command
PARAMETER repeat_penalty 1.1

# Context window: 16k optimal for 14B (has better attention than 8B)
# 14B can handle larger contexts effectively
PARAMETER num_ctx 16384

# M4 PERFORMANCE SETTINGS
# Force all layers to Neural Engine/GPU for max speed
PARAMETER num_gpu 99

# NO SYSTEM PROMPT - all instructions sent from code to avoid conflicts