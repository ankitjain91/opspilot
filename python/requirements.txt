# LangGraph K8s Agent Server Dependencies (Runtime)
# Install: pip install -r requirements.txt
#
# NOTE: This file is for the RUNTIME agent-server binary.
# For embedding generation, see requirements-embeddings.txt

# Core LangGraph
langgraph>=0.2.0
langchain-core>=0.3.0

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# HTTP Client
httpx>=0.27.0

# Kubernetes Async Client (For Sentinel/Watch)
kubernetes_asyncio>=28.0.0

# Server-Sent Events for streaming
sse-starlette>=2.0.0

# Build Tools (for creating standalone binary)
pyinstaller>=6.0.0
